extends layout

block content
 div.headerContent
  h1.title #{siteTitle} 
  div.searchInput
   form(method='GET' action='/search')
    input#search(type='text' placeholder='Search for a job' name='keyword')
    button#searchButton Search
 h3.websiteTitle Jobs found on Posao.hr
 ul.posaoList
  for val in title
   li.listItems
    a(href= val.posao_links)= val.posao_job
 h3.websiteTitle Jobs found on MojPosao.net
 ul.mojPosaoList
  for val in titleTwo
   li.listItems
    a(href= val.mojposao_links)= val.mojposao_job
 h4.websiteTitle Featured jobs on MojPosao.net
 ul.featuredList
  for val in titleTwo
   li.listItems
    a(href= val.featured_links)= val.featured_job

